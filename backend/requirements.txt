# ── Núcleo de la API ──────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ── Base de datos: PostgreSQL con SQLAlchemy 2.x async ────────────────────
sqlalchemy>=2.0
psycopg[binary]>=3.2        # driver PostgreSQL (async) — reemplaza a pymysql
greenlet>=3.0               # requerido por SQLAlchemy en modo async

# ── Validación y configuración ────────────────────────────────────────────
pydantic>=2.7
pydantic[email]>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ── Seguridad: JWT y hashing de contraseñas ───────────────────────────────
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt==4.0.1               # fijado: passlib 1.7.4 no soporta bcrypt>=4.1
slowapi>=0.1.9              # limita intentos de login por IP

# ── Integraciones externas ────────────────────────────────────────────────
httpx>=0.27                 # cliente HTTP async (Groq)
stripe>=10.0                # pasarela de pago

# ── Reportes y documentos ─────────────────────────────────────────────────
reportlab>=4.0              # facturas y reportes en PDF
openpyxl>=3.1               # reportes en Excel (.xlsx)

# ── IA local (sugerencia de precios) ──────────────────────────────────────
scikit-learn>=1.4
joblib>=1.4
numpy>=1.26

# ── Pruebas ───────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20             # SQLite en memoria para la suite de pruebas
